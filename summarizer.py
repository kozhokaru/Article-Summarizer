# article_summarizer/summarizer.py

def summarize_url(url: str) -> str:
    """
    Given a URL, fetch and summarize it.
    (Implementation to come in later commits.)
    """
    return f"(Summary for {url} goes here)"
